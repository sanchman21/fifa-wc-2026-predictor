# Pinned to a known-good, mutually-compatible set (verified end-to-end on Python 3.12).
# Streamlit Community Cloud installs fresh from this file, and the previous unpinned
# `>=` floors let it resolve untested combinations (e.g. numpy 2.x / pandas 3.x) at build
# time — the source of the read-only/run-time crashes after redeploys. Bump deliberately,
# re-test the forecast, then move these pins together.
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
matplotlib==3.9.2
streamlit==1.37.1
altair==5.0.1
beautifulsoup4==4.12.3
lxml==5.2.1
